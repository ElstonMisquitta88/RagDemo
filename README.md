# RagDemo

"I read PDFs, chunked the content, generated embeddings locally using AllMiniLM, stored the chunk text and embeddings in SQL Server, and implemented semantic search using cosine similarity to retrieve the most relevant chunks."

RagDemo

├── Program.cs

├── PdfReader.cs

├── EmbeddingService.cs

├── SqlHelper.cs

├── SimilarityHelper.cs

├── Chunk.cs

└── Sample.pdf
